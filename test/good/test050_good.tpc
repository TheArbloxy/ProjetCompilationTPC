// Test 50 (While 2)

int callWhile(int x) {
    return x + 2;
}

int main(void) {
    int a;
    a = 0;

    while (a < 100) {
        a = callWhile(a);
    }

    putint(a);
    putchar('\n');

    return 0;
}
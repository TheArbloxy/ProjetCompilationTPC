// Test 51;

int x;
int y;
int z;

int main(void) {
    x = 10;
    y = 5;
    z = 13;

    x = !(x > y || x > z);
    if (x) {
        putint(1);
    } else {
        putint(0);
    }
    putchar('\n');

    /*
    if (a > b) {
        putint(1);
    } else {
        putint(0);
    }
    putchar('\n');

    if (a <= b) {
        putint(1);
    }
    putchar('\n');

    if (a >= b) {
        putint(1);
    }
    putchar('\n');
    */
}